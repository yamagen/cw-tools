# TODO: cw-tools 21代集対応

## 方針

当面は、現在の古今集による観察を続け、データ修正と viewer / cw
の熟成を優先する。21代集対応は、その観察が十分に進んでから実装する。

ただし、`all-v02-21daishu.db` には21代集までのデータと BG/CH code
があり、分割単位2段階・表記同一視2段階の観察が可能であるため、将来の拡張方針をここに記録する。

## 現在の状態

-   元データ: `all-v02-21daishu.db`
-   現在の cw corpus は八代集（anthology 1--8）から生成。
-   PHP viewer (`templates/cw-suite.php`) は現在、古今集のみを抽出。
-   既定 corpus: `tests/data/hachidaishu-bg-split.txt`
-   既定 IDF: `tests/data/hachidaishu-bg-16-split.idf`
-   現在の既定観察条件: `古今集 × split × first 16`
-   `tests/data/` には full / first-16 × split / non-split の4種の IDF
    がある。

## 将来の PHP control

観察条件を次の3軸として明示的に選択できるようにする。

### 1. Anthology

古今集から新続古今集まで21代集を選択可能にする。将来的には単独歌集だけでなく範囲指定も検討する。

現在の song ID は、anthology 1 の `10142` (= 1:0142) と anthology 10 の
`100142` (= 10:0142) のような形式なので、単純な `grep '^1'`
では区別できない。歌集番号の抽出は桁数を含めて厳密に行う。

### 2. Segmentation

-   `whole` / non-split
-   `split`

例: whole では `音羽山`、split では `音羽 / 山`。

D/E 階層については変換側に個別例外を持たせず、修正済み
`all-v02-21daishu.db` の階層をそのまま利用する。

### 3. Identity

-   `full`
-   `first 16`

`first 16` は BG/CH code
の先頭16文字を同一性判定に用い、細かな表記差を問わない観察に対応する。

## Corpus / IDF

21代集対応時には21代集全体を母体とする次の corpus / IDF
を生成することを検討する。

-   `21daishu-bg.txt`
-   `21daishu-bg-split.txt`
-   `21daishu-bg.idf`
-   `21daishu-bg-split.idf`
-   `21daishu-bg-16.idf`
-   `21daishu-bg-16-split.idf`

corpus と IDF の segmentation は必ず一致させる。identity も full ↔ full
IDF、first 16 ↔ first-16 IDF / `--substr 16` と対応させる。

## PHP 実装時

### `templates/index.php`

-   Request panel に `anthology`, `segmentation`, `identity` を追加。
-   `substr` の数値入力は、通常UIでは `full / first 16`
    として意味を見せ、内部で `--substr` に変換することを検討。
-   既定値 `古今集 × split × first 16` は維持。

### `templates/cw-suite.php`

-   anthology / segmentation / identity を request から受け取る。
-   segmentation に応じて corpus を選択。
-   segmentation × identity に応じて IDF を選択。
-   anthology を安全に抽出し、現在の `grep '^1'` に依存しない。
-   不正値や corpus / IDF の不整合を拒否する。

## 先に行うこと

21代集 UI
を急いで実装しない。まず古今集で次を十分に観察し、データと観察系を修正・熟成する。

-   D/E 階層と split の妥当性
-   BG/CH code による語の同一性
-   free-key の粗い検索とネットワーク上の分離
-   Force / KK / BFS の観察上の差
-   Source texts への復帰
-   Z, α, β その他の viewer 操作

今回の例では、`音羽山 → 音羽 / 山` が正しく反映され、`津` の検索から
`津の国` と `御津` が別の関係世界として現れた。原歌へ戻れば、読者には
`御津 / 見つ`、`浮き海藻 / 憂き目` の掛詞も見える。

cw
は掛詞を自動的に決定・ラベル付けする必要はない。計算は関係構造を提示し、Source
texts に戻る経路を提供する。解釈は読者の観察に残す。

## 原則

> 機械は発見可能な位置まで連れていく。意味を一つに確定しない。

21代集対応も、この観察装置としての性格を維持したまま行う。
